# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Mod键的设置（Mod4是win键，Mod1是Alt键）
set $mod Mod4

# i3除status bar以外的颜色定义
# class                 border  backgr. text    indicator child_border
client.focused          #333333 #f28500 #ffffff #2e9ef4   #285577
client.focused_inactive #333333 #575e60 #ffffff #484e50   #5f676a
client.unfocused        #333333 #333333 #888888 #292d2e   #222222
client.urgent           #2f343a #c70000 #ffffff #c70000   #c70000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

# 禁用窗口边框，i3-gaps推荐设置
for_window [class=".*"] border pixel 0

# 设置窗口间距
gaps inner 6
gaps outer 2
# 窗口距离屏幕边缘的距离为inner+outer

# Only enable gaps on a workspace when there is at least one container
# 智能间隙，只在工作区至少有一个容器时启用outer间隙
smart_gaps on

# Only enable outer gaps when there is exactly one container
# 智能间隙，只在工作区只有一个容器时启用outer间隙
#smart_gaps inverse_outer

# Activate smart borders (always)
# 智能边框，当工作区有多于一个窗口时才启用窗口边框
#smart_borders on

# Activate smart borders (only when there are effectively no gaps)
# 智能边框，当窗口间没有间隙时才启用窗口边框
#smart_borders no_gaps

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# 窗口标题字体
font pango:DejaVu Sans 12

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# 启动锁屏程序
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock-fancy -g -f DejaVu-Sans

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# 启动nm-applet
exec --no-startup-id nm-applet

# 启动volumeicon
exec --no-startup-id volumeicon

# 启动fcitx
exec --no-startup-id fcitx -d

# 启动blueman-applet
exec --no-startup-id blueman-applet

# 启动picom，参数中指定glx为后端是为了解决i3wm的双倍缓冲带来的画面撕裂现象
exec --no-startup-id picom --backend glx -b

# Use pactl to adjust volume in PulseAudio.
# 绑定键位使用pactl调整PulseAudio的音量
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# 绑定键位更改屏幕亮度
bindsym XF86MonBrightnessUp exec --no-startup-id sudo light -A 10
bindsym XF86MonBrightnessDown exec --no-startup-id sudo light -U 10

# Use Mouse+$mod to drag floating windows to their wanted position
# 使用鼠标+mod键拖动浮动窗口到想要的位置
floating_modifier $mod

# start a terminal
# 启动终端，因为alacritty不支持启动提醒，所以加上--no-startup-id参数
bindsym $mod+Shift+Return exec --no-startup-id alacritty

# 一些程序的绑定键
bindsym $mod+Shift+f exec firefox
bindsym $mod+Shift+t exec thunar
bindsym $mod+Shift+m exec --no-startup-id netease-cloud-music

# 截图键绑定shutter，PrtSc键截取整个屏幕，mod键+PrtSc键选择截图区域
bindsym Print exec shutter -f -e -o ~/Pictures/screenshot/%Y-%m-%d-%T.png
bindsym $mod+Print exec shutter -s -e -o ~/Pictures/screenshot/%Y-%m-%d-%T.png

# kill focused window
# 关闭当前窗口
bindsym $mod+q kill

# start dmenu (a program launcher)
# 绑定键位启动dmeno应用程序启动器
bindsym $mod+Return exec --no-startup-id dmenu_run -fn 'Noto Sans Mono-14' -sb '#ffa500'

# A more modern dmenu replacement is rofi:
#bindcode $mod+Return exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
# 改变焦点
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
# 改变焦点的替代键位
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# 移动聚焦的窗口
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
# 移动聚焦窗口的替代键位
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# 水平分割窗口
bindsym $mod+minus split h

# split in vertical orientation
# 垂直分割窗口
bindsym $mod+backslash split v

# enter fullscreen mode for the focused container
# 将当前聚焦的容器进入全屏模式
bindsym $mod+plus fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# 改变容器布局（stacking堆叠式，tabbed标签式，toggle split在两种容器分割方式之间切换）
bindsym $mod+s layout stacking		
bindsym $mod+t layout tabbed		
bindsym $mod+g layout toggle split	 

# toggle tiling / floating
# 切换平铺和浮动式
bindsym $mod+f floating toggle

# change focus between tiling / floating windows
# 在平铺和浮动窗口之间切换焦点
bindsym $mod+space focus mode_toggle

# focus the parent container
# 聚焦到父容器
bindsym $mod+a focus parent

# focus the child container
# 聚焦到子容器
bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
# 工作区名称
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
#set $ws7 "7"
#set $ws8 "8"
#set $ws9 "9"
#set $ws10 "10"

# switch to workspace
# 切换工作区
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
#bindsym $mod+7 workspace number $ws7
#bindsym $mod+8 workspace number $ws8
#bindsym $mod+9 workspace number $ws9
#bindsym $mod+0 workspace number $ws10

# move focused container to workspace
# 把聚焦的容器移动到其它工作区
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
#bindsym $mod+Shift+7 move container to workspace number $ws7
#bindsym $mod+Shift+8 move container to workspace number $ws8
#bindsym $mod+Shift+9 move container to workspace number $ws9
#bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
# 重新载入配置文件
bindsym $mod+Shift+r reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# 重启i3
#bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
# 退出i3（登出 X 会话）
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
# 改变窗口大小（可以用鼠标）
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
# 进入resize模式
bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# 启动状态栏
bar {
	font pango:DejaVu Sans Mono,FontAwesome
	# 在顶部显示状态栏
	position top
        # bar的颜色设置
        colors {
        background #363636
        statusline #ffffff
        separator #666666
        #<colorclass>      <border> <background> <text>
        focused_workspace  #333333  #00a693     #ffffff
        active_workspace   #333333  #5f676a     #ffffff
        inactive_workspace #333333  #222222     #888888
        urgent_workspace   #2f343a  #f21414     #ffffff
        binding_mode       #2f343a  #2e68cc     #ffffff
	}

	# 以下为bumblebee status的设置
        #status_command bumblebee-status \
	#	-m indicator network_traffic cpu2 memory battery date time \
	#	-p indicator.include=capslock cpu2.layout=cpu2.cpuload memory.interval=1s memory.format='{used}' date.format="%b%d日 星期%a" time.format="%p%I:%M" \
	#	-t dracula-powerline
	# status_command 后接bumblebee-status的可执行文件路径
	# -m 加载的模块，按顺序显示
	# -p 模块的参数
	# -t 使用的主题
	
	# i3status-rust
	status_command i3status-rs ~/.config/i3status-rust/config.toml
}
